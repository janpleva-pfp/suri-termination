apiVersion: v1
kind: Service
metadata:
  name: {{ include "pfp-terminate.fullname" . }}
  labels:
    {{- include "pfp-terminate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pfp-terminate.selectorLabels" . | nindent 4 }}
